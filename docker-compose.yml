services:
  lab:
    build:
      context: .
      dockerfile: docker/Dockerfile
    working_dir: /workspace
    tty: true
    stdin_open: true
    volumes:
      - ./:/workspace
    command: sleep infinity
