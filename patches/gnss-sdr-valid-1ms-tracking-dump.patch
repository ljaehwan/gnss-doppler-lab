diff --git a/src/algorithms/tracking/gnuradio_blocks/dll_pll_veml_tracking.cc b/src/algorithms/tracking/gnuradio_blocks/dll_pll_veml_tracking.cc
--- a/src/algorithms/tracking/gnuradio_blocks/dll_pll_veml_tracking.cc
+++ b/src/algorithms/tracking/gnuradio_blocks/dll_pll_veml_tracking.cc
@@ -2124,10 +2124,13 @@ int dll_pll_veml_tracking::general_work(int noutput_items __attribute__((unused)
                         run_dll_pll();
                         update_tracking_vars();
                         check_carrier_phase_coherent_initialization();
+                        // ACAF-NF additive exporter: the GPS L1 C/A correlator and
+                        // loop state are valid every 1 ms here.  The stock dump is
+                        // navigation-bit gated even when coherent integration is 1 ms.
+                        // This changes dump cadence only; telemetry gating is unchanged.
+                        log_data();
                         if (d_current_data_symbol == 0)
                             {
-                                // enable write dump file this cycle (valid DLL/PLL cycle)
-                                log_data();
                                 // ########### Output the tracking results to Telemetry block ##########
                                 // Fill the acquisition data
                                 current_synchro_data = *d_acquisition_gnss_synchro;
