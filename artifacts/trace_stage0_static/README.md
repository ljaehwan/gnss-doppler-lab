# TRACE Stage-0 static artifacts

Configuration frozen before this TRACE evaluation.

This directory binds the TRACE analytic-plus-linear implementation to genuine
TEXBAT and OAKBAT complex nine-tap receiver outputs. `input_inventory.json` and
`source_binding.json` record actual fields and SHA-256 provenance;
`alignment_validation.json` records the row-t action to row-(t+1) correlator
proof. Phase B result tables, diagnostics, controls, plots, checksums, and the
single allowed final verdict are added from the frozen commit.

The method is claimable only as normal-only action-conditioned prediction of
subsequent complex correlator evolution in the static coherent carry-off scope.
It is not a B0 replacement and does not establish general spoofing detection.
