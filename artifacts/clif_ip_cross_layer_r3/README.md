# CLIF-IP cross-layer R3 artifacts

## Verdict: **NO-GO**

1. **B0 freeze.** existing B0 architecture retrained/frozen on CLIF clean train: shared PRN-local GRU, signed nine-tap target, no PRN ID. Model and standardizer fit inputs are whole-contained in 0--240 s (max end 239.7 s); the previously published B0 remains a separate comparator and was not modified.
2. **M1 definition.** M1-style surrogate over actual raw-IQ features (not an original M1 checkpoint). Frozen hash `e192ea2d410d836dc2764b1d9c576be6680f8c51e5d358f4223e03da03d07887`; exactly 1 clean fit and 480 whole-contained rows.
3. **Leakage/reset.** Validation 250--330 s, independent clean test 340+ s, and os1--os4 are transform-only. Every split/recording resets M1/B0/downstream history, so B0 and all downstream states see no independent-test rows during fit.
4. **Predictor protocol.** P0--P3 share signed nine-tap targets and support. Common validation-selected history lag=2 epochs (1 s); alphas {'P1': 100.0, 'P2': 100.0, 'P3': 100.0}. `lag_analysis.csv` separately labels causal-alignment shifts and predictor-history lag.
5. **Independent prediction.** P1 test MSE=0.0104566329; P3=0.0106082819; P3-vs-P1=-1.450%. No incremental improvement; this is a failure for the cross-layer prediction claim.
6. **Ablations/calibration.** Validation-only exact-set recalibration: Full=[B0,M1,P3,concordance], -M1=[B0,P1] (no M1/P3/concordance dependency), -B0history=[M1,P2] (no B0/concordance), -concordance=[B0,M1,P3].
7. **Detection/alignment.** Full mean ROC-AUC=0.8943, PR-AUC=0.9562, independent clean-test FPR=0.0226. Nineteen permutations imply p-value resolution 0.05; reported 95% bounds are permutation-delta quantiles, not sampling CIs. `available_s` delays are processing-time proxies; physical delay cannot be claimed without receiver-delay/common-anchor verification.
8. **Provenance/checksums.** B0/M1 CSV and checkpoint hashes were computed live. os1--os4 raw-IQ values are cached canonical digests; current path/stat was recorded but digest was not reverified this run. cleanStatic remains provisional/null. Final checksums (including `test_summary.txt`, excluding the manifest itself) are written only after tests.
9. **Claim boundary/files.** No physical causality, exact sample alignment, original-M1 performance, cross-corpus generalization, or universal superiority claim. Bundle: `config.json`, `provenance_manifest.json`, `frozen_m1_fit_summary.json`, `clif_b0_prn_local_gru.pt`, `clif_b0_training_metadata.json`, `clif_b0_training_history.csv`, predictor/hyperparameter/scenario/fusion/ablation/lag CSVs, per-epoch CSVs, destruction JSON, plots, and `test_summary.txt`.
