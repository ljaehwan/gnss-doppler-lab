# CLIF-IP Phase 1 R2 artifacts

Provenance-only blocked report. No result CSV, plot, metric, frozen state, or evaluator output is present. `provenance_manifest.json` is the machine-readable fail-closed gate record; `test_summary.txt` records verification.
