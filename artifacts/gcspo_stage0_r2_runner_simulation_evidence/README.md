# GCSPO Stage-0 R2 runner simulation

This directory contains an independently recomputed, frozen Stage-0 evaluation under the research runner. It does not reuse old metric CSVs. Large epoch-level products remain here on SSD.

The detector verdict and the shared-pull-off physics verdict are reported separately in `final_verdict.json`.
